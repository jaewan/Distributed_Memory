# cp (and remove existing) files for spark.conf and hadoop.conf.

# start hadoop, spark, and yarn instance.

# run sparkbench with pure memory option.

# ??? save results to separate folder? --> /results/m

# run sparkbench with pure disk option.

# ??? save results to another separate folder? --> /results/d
